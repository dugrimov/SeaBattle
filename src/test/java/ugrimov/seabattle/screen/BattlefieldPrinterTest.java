package ugrimov.seabattle.screen;

import java.util.Collections;
import java.util.List;
import org.junit.jupiter.api.Test;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.verify;
import ugrimov.seabattle.domain.Battlefield;
import ugrimov.seabattle.domain.Ship;

public class BattlefieldPrinterTest {

    @Test
    public void testPrint() {
        var mapperMock = mock(BattlefieldCellMapper.class);
//        var battlefield = new Battlefield(List.of(new Ship(Collections.EMPTY_LIST)), null);
//        var printer = new BattlefieldPrinter();
//        var result = printer.print(battlefield, mapperMock);
//        verify(mapperMock
    }
}
